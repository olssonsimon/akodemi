<% provide(:title, "Nytt jobb") %>

<h1>Nytt jobb</h1>

<div class="row">
	<div class="span6 offset2">
		<%= form_for @job, html: { class: "form-horizontal" } do |f| %>
			
			<%= render 'fields', f: f %>
			<%= f.submit 'Skapa nytt jobb', class: "btn btn-primary", id: "form-button" %> 
		<% end %>
	</div>
</div>